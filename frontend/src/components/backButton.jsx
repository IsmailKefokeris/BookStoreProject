import React from "react";
import { Link } from "react-router-dom";
import { BsArrowLeft } from "react-icons/bs";

const backButton = ({ destination = "/" }) => {
    return <div className="flex">backButton</div>;
};

export default backButton;
